DROP TABLE users;
CREATE TABLE users(id INT PRIMARY KEY UNIQUE NOT NULL, email_hash TEXT NOT NULL, password_key TEXT NOT NULL, user_data_encrypted TEXT NOT NULL, confirmed boolean DEFAULT false);
CREATE INDEX on users (email_hash);